<%= simple_form_for @artist, :html => { :multipart => true }  do |f| %>
	<%= select_tag(:genre_id, options_for_select(@genre), :prompt => t('.select_genre')) %>
	<%= f.file_field :artist_img%>
	<%= f.input :title, label: t('.artist_name') %>
	<%= f.input :biography %>	 
	<%= f.button :submit %>
<% end %>

<!-- This form will be rendered when a user wants to create a new artist -->